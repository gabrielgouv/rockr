{
  "name": "@rockr/rockr-vm",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@rockr/rockr-core": "^1.0.0",
    "@rockr/rockr-generate-script": "^1.0.0",
    "argparse": "^2.0.1",
    "express": "^4.17.1",
    "js-yaml": "^4.0.0",
    "yaml": "^2.0.0-3"
  },
  "devDependencies": {
    "typescript": "~4.1.3"
  }
}
