{
    "name": "@rockr/rockr-generate-script",
    "version": "1.0.0",
    "main": "dist/index",
    "types": "dist/index",
    "files": [
      "dist"
    ],
    "scripts": {
      "build": "yarn clean && yarn compile",
      "clean": "rm -rf ./dist",
      "compile": "tsc -p tsconfig.build.json"
    },
    "dependencies": {
      "@rockr/rockr-core": "^1.0.0"
    },
    "devDependencies": {
      "@types/node": "^15.0.1",
      "typescript": "~4.1.3"
    }
  }